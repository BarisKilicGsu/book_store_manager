import { StoreManagerStoreEntity } from './store-manager-store.entity';

describe('StoreManagerStoreEntity', () => {
  it('should be defined', () => {
    expect(new StoreManagerStoreEntity()).toBeDefined();
  });
});
