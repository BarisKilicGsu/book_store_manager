import { BookstoreBookEntity } from './bookstore-book.entity';

describe('BookstoreBookEntity', () => {
  it('should be defined', () => {
    expect(new BookstoreBookEntity()).toBeDefined();
  });
});
